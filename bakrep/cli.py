import argparse

def main():
  parser = argparse.ArgumentParser(
                    add_help=True,
                    prog = 'bakrep',
                    description = 'A tool to download bakrep datasets')

if __name__ == "__main__":
  main()
